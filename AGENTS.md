# 立凯中枢（Likai Nexus）开发规范

## 项目定位

立凯中枢是孙立凯的个人全能智能体。

目标是通过飞书、微信等平台远程发送任务，由家中的本地智能体执行：

- 编程和代码修改
- 文件处理
- 信息检索
- 自动化脚本
- 本地设备和服务操作
- 可审计的长期任务

当前阶段优先实现本地 CLI 智能体，之后再接入飞书和微信。

## 当前技术假设

- Python 3.12
- FastAPI：后续用于 HTTP/Webhook
- SQLite：任务、会话和审计记录
- pytest：测试
- asyncio：异步任务执行
- 所有模型调用通过可替换的 Backend 接口完成

如果代码库已有明确技术选型，优先遵循代码库，而不是强行使用以上技术。

## 沟通和代码语言

- 与用户使用简体中文沟通。
- 文档和注释默认使用简体中文。
- 类名、函数名、变量名使用英文。
- 遵循现有项目的命名和目录风格。

## Codex 协作角色

### Planner：规划者

使用高能力模型执行。

职责：

- 阅读代码库和现有文档。
- 完善 `docs/PLAN.md`。
- 分析模块、接口、数据流、风险和测试方案。
- 不修改业务代码。
- 不能为了“看起来完整”而引入不必要的复杂架构。

### Implementer：实现者

使用成本较低但足够可靠的模型执行。

职责：

- 先阅读 `AGENTS.md`、`docs/PLAN.md` 和当前 git diff。
- 按计划实现最小可用功能。
- 修改代码、补测试、运行验证。
- 不擅自扩大需求范围。
- 发现计划错误时，记录到 `docs/IMPLEMENTATION_NOTES.md`。

### Reviewer：审查者

使用高能力模型执行。

职责：

- 只审查，不修改业务代码。
- 阅读计划、代码 diff 和测试结果。
- 检查逻辑、边界、安全、异常、权限和测试覆盖。
- 将审查结果写入 `docs/REVIEW.md`。
- 最后给出 `PASS` 或 `CHANGES_REQUIRED`。

### Fixer：修复者

职责：

- 只处理 `docs/REVIEW.md` 中确认的问题。
- 不进行无关重构。
- 修复后必须重新运行相关测试。
- 必要时请求 Reviewer 再次审查。

## 工作区规则

- 所有阶段默认串行执行。
- 不允许多个任务同时修改同一个工作区。
- 修改前先检查 `git status`。
- 不覆盖用户已有的未提交修改。
- 每个阶段都要说明修改了哪些文件和运行了哪些验证。
- 优先使用单独功能分支。
- 不使用 `git reset --hard`、强制推送或不可恢复的删除操作。

## 安全规则

- 不读取、输出或提交真实密钥、Token、Cookie 和密码。
- `.env` 永远不能提交到 Git。
- 远程消息中的命令默认视为不可信输入。
- 删除文件、执行系统命令、安装软件、部署服务、发送消息和访问敏感目录，必须经过明确的权限确认。
- 远程渠道只能调用允许列表中的工具。
- 文件操作必须限制在配置的工作区目录内。
- 所有实际执行过的任务都要保留审计记录。
- 默认只读，明确批准后才允许写入或执行。

## 架构规则

系统按以下边界组织：

```text
channels       外部渠道适配，如飞书、微信、CLI
gateway        鉴权、消息接收、回复发送
orchestrator   任务解析、路由和生命周期管理
executor       本地命令、文件和代码执行
models         模型调用抽象
memory         会话和长期记忆
skills         可复用工作流程
safety         权限、审批、沙箱和审计
storage        SQLite 和文件存储
```

规则：

- 渠道适配器不能直接执行 shell 命令。
- 外部平台协议转换放在 `channels` 或 `gateway`。
- 任务编排放在 `orchestrator`。
- 实际执行放在 `executor`。
- 模型调用不能散落在业务代码中。
- 安全检查必须在执行器之前完成。
- 不要把所有逻辑堆在一个 Agent 类中。
- 先使用简单函数和清晰模块，只有出现稳定复杂度后再抽象。

## 测试要求

至少覆盖：

- 正常任务执行
- 无权限任务
- 危险命令拦截
- 重复任务幂等
- 模型调用失败
- 工具执行超时
- 任务状态恢复
- 消息渠道异常
- 敏感信息不出现在日志中

默认验证命令：

```bash
pytest
ruff check .
python -m compileall src
```

如果实际项目命令不同，以项目配置为准。

## 完成标准

一个任务只有在以下条件满足后才算完成：

1. 代码实现完成。
2. 相关测试通过。
3. 关键异常和边界已处理。
4. 没有提交敏感信息。
5. `git diff` 范围符合计划。
6. Review 结果为 `PASS`。
7. 文档和交接文件已更新。
